
<?php
    $username = "TestUser"; 
    $phone = "123-456-7890";
    $array = ["this", "is", "it"];
    // $username = strtolower($username); // converts to lower case
    // $username = strtoupper($username); // converts to upper case
    // $username = trim($username); // removes whitespaces
    // $username = str_pad($username, 20 , 0); // appends desired number of character in the string
    // $phone = replace("-", "", $phone); // replaces the given character with desired character and returns a new string
    // $username = strrev($username); // converts to reverse
    // $username = str_shuffle($username); // converts to shuffled manner
    // $equals = strcmp($username, $username); // returns true if strings match otherwise false
    // $count = strlen($phone); // returns the length of string
    // $index = strpos($phone, '-'); // returns the index of given character if it exists in the string
    // $fullname = explode(" ", $username); // converts the string to an array on the basis of given seperating character
    // $sub = substr($username, 0, 4); // returns a sub string within the range provided in paramenters
    // $str = implode(" ", $array); // converts the array to string
    $echo = "reserved word<br>";
    echo $echo;



?>